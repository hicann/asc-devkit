# max

<!-- md-trans-meta sourceCommit=c17b32aa8d725bcb43135ab25add5a26d62ad5c0 translatedAt=2026-09-06T12:38:30.904Z pushedAt=2026-09-11T04:55:42.363Z -->

## Applicable Products

- Ascend 950PR/Ascend 950DT: supported
- Atlas A3 training products/Atlas A3 inference products: supported
- Atlas A2 training products/Atlas A2 inference products: supported
- Atlas 200I/500 A2 inference products: not supported
- Atlas inference products AI Core: supported
- Atlas inference products Vector Core: not supported
- Atlas training products: not supported

## Description

Compares two numbers of the same data type and returns the maximum value.

## Prototype

```
template <typename T, typename U>
__aicore__ inline T max(const T src0, const U src1)
```

## Parameters

**Table 1** Template parameter description

| Parameter | Description |
| --- | --- |
| T | Data type of the input data **src0**. Currently supported data types are bool, int8_t, uint8_t, int16_t, uint16_t, int32_t, uint32_t, float, int64_t, and uint64_t. |
| U | Data type of the input data **src1**. Currently supported data types are bool, int8_t, uint8_t, int16_t, uint16_t, int32_t, uint32_t, float, int64_t, and uint64_t.<br><br>Reserved type, which must be consistent with **T**. |

**Table 2** API parameter description

| Parameter | Input/Output | Description |
| --- | --- | --- |
| src0 | Input | Source operand. Input that participates in the comparison. |
| src1 | Input | Source operand. Input that participates in the comparison. |

## Constraints

The data types of the two source operands must be the same.

## Return Value

Maximum of the two input data.

## Example

```
int64_t src0 = 1;
int64_t src1 = 2;

int64_t result = AscendC::Std::max(src0, src1);
// result: 2
```

